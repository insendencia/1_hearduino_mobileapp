<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.hearduino_mobileapp">

    <!-- permission to access internet from the app -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Hearduino_mobileApp"
        tools:targetApi="31">

        <activity
            android:name=".Popout_RelationToDeaf"
            android:parentActivityName=".Popout_Dyk"/>

        <activity
            android:name=".Popout_Dyk"
            android:parentActivityName=".Activity_SignUp"/>

        <activity
            android:name=".Activity_SignUp2"
            android:parentActivityName=".Activity_SignUp"/>

        <activity
            android:name=".Activity_SignUp"
            android:parentActivityName=".Popout_SignUpWGoogle"/>

        <activity
            android:name=".Popout_SignUpWGoogle"
            android:parentActivityName=".Activity_Login"/>

        <activity
            android:name=".Popout_LoginWGoogle"
            android:parentActivityName=".Activity_Login"/>

        <activity
            android:name=".Activity_Login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>